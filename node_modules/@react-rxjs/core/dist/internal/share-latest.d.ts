import { Observable, noop } from "rxjs";
import { BehaviorObservable } from "./BehaviorObservable";
declare const shareLatest: <T>(source$: Observable<T>, defaultValue: T, shouldComplete?: boolean, teardown?: typeof noop) => BehaviorObservable<T>;
export default shareLatest;
