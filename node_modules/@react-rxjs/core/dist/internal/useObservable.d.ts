import { SUSPENSE } from "../SUSPENSE";
import { BehaviorObservable } from "../internal/BehaviorObservable";
import { Subscription } from "rxjs";
export declare const useObservable: <O>(source$: BehaviorObservable<O>, subscription?: Subscription | undefined) => Exclude<O, typeof SUSPENSE>;
